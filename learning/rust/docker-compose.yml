services:
  rust:
    image: rust
    container_name: rust
    build:
      context: .
    volumes:
      - ./:/usr/app/
      # both for zed remote
      # - ~/.ssh/:/root/.ssh/:ro
      # - ~/.config/zed:/root/.config/zed:ro
      
    ports:
      # for zed remote
      # - "2222:22"
